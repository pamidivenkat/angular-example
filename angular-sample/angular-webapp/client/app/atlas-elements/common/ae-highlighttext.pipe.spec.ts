import { AeHighlighttextPipe } from './ae-highlighttext.pipe';

describe('AeHighlighttextPipe', () => {
  it('create an instance', () => {
    const pipe = new AeHighlighttextPipe();
    expect(pipe).toBeTruthy();
  });
});
