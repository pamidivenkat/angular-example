import { Injectable } from '@angular/core';
import { TranslationHandler } from 'angular-l10n';
@Injectable()
export class TranslationHandlerStub  {
    // parseValue(path: string, key: string, value: string | null, args: any, lang: string): string {
    //     return '';
    // }
}
